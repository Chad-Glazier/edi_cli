# TODO

- Implement UI components.
- Generalize the `run` command.
- When the analytics are implemented in the core module, create a command to visualize them in the CLI.
	- Include means of exporting the data to CSV files.
